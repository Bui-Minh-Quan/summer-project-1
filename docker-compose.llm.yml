services:

  vllm:
    image: vllm/vllm-openai:v0.10.1
    container_name: vllm
    restart: unless-stopped
    runtime: nvidia
    ipc: host

    dns:
      - 8.8.8.8
      - 1.1.1.1

    ports:
      - "8008:8000"

    environment:
      HUGGING_FACE_HUB_TOKEN: ${HF_TOKEN:-}

    volumes:
      # PERSIST HF CACHE: Prevents downloading model weights on container restart
      - ~/.cache/huggingface:/root/.cache/huggingface

    command: >
      --model Qwen/Qwen2.5-1.5B-Instruct-GPTQ-Int4
      --served-model-name qwen-1.5b
      --dtype auto
      --max-model-len 8192
      --gpu-memory-utilization 0.85
      --enforce-eager
      --guided-decoding-backend xgrammar
      --enable-chunked-prefill
      --max-num-seqs 8

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities:
                - gpu

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 10
    
    networks:
      - financial-ai-network  

networks:
  financial-ai-network:    
    external: true